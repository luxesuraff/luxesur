---
const { city } = Astro.props;

const intents = [
  {
    label: "Emergency Plumbing",
    path: `/emergency-plumbing/${city.slug}`,
  },
  {
    label: "Burst Pipe Repair",
    path: `/emergency-plumbing/burst-pipe/${city.slug}`,
  },
  {
    label: "Sewer Backup Cleanup",
    path: `/emergency-plumbing/sewer-backup/${city.slug}`,
  },
  {
    label: "No Hot Water Repair",
    path: `/emergency-plumbing/no-hot-water/${city.slug}`,
  },
];
---

<section style="margin-top: 48px;">
  <h2>Related Emergency Plumbing Services in {city.city}, {city.state}</h2>

  <ul>
    {intents.map((intent) => (
      <li>
        <a href={intent.path}>{intent.label}</a>
      </li>
    ))}
  </ul>
</section>
